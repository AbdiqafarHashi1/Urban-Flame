

services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: urbanflamebackend
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development

  db:
    image: postgres:15
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER:   urban
      POSTGRES_PASSWORD: flame
      POSTGRES_DB:     urban_flame
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
